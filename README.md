🏗️ Fimarek Web Engine v2.0

Fimarek Oy / OÜ — Инженерное решение для веб-представительства строительной компании в регионе Uusimaa. Переход от одностраничного лендинга к модульной многостраничной архитектуре с упором на производительность, чистоту кода и мультиязычность.
🎯 Цели проекта

    Визуальный апгрейд: Переход к современному индустриальному дизайну (видео-фоны, четкая сетка).

    Модульность: Разделение сайта на 4 независимые страницы с общими компонентами (Header/Footer).

    SEO & Домены: Оптимизация под два региональных домена (.fi, .ee) с автоматическим определением языка.

    Легкость: Отказ от тяжелых фреймворков в пользу микро-библиотек.

🛠 Технологический стек

    Core: HTML5, CSS3, TypeScript / JavaScript (ES6+).

    UI Frameworks:

        Skeleton CSS — ультра-легкая сетка (Grid System) для адаптивности.

        Pure CSS — готовые стили для кнопок, форм и навигации.

    Architecture: Vanilla Components (динамическая подгрузка HTML-модулей через Fetch API).

    i18n: Кастомная система локализации через translations.js.

📂 Структура файлов
code Text

/
├── index.html              # Главная страница (Hero, Услуги, Объекты)
├── about.html              # О нас (История, Команда, Опыт)
├── portfolio.html          # Портфолио (Галерея выполненных работ)
├── connect.html            # Связаться (Форма, Контакты, Карта)
│
├── partials/               # Общие модули (подгружаются динамически)
│   ├── header.html         # Шапка с навигацией и выбором языка
│   └── footer.html         # Футер с контактами и копирайтом
│
├── css/
│   ├── skeleton.css        # Сетка и база
│   ├── pure-min.css        # Кнопки и формы
│   └── main.css            # Кастомные стили (цвета бренда, анимации)
│
├── js/
│   ├── main.js             # Инициализация сайта и загрузка компонентов
│   ├── translations.js     # Словарь (FI, ET, RU, EN) и логика перевода
│   └── gallery.js          # Скрипты для работы портфолио
│
├── materials/              # Исходные медиа-файлы
│   ├── video/              # Фоновое видео (fimarektest.mp4)
│   ├── images/             # Фотографии проектов (WebP/Optimized)
│   └── logo/               # Логотипы (SVG/Vector)
│
└── README.md               # Документация проекта

⚙️ Функциональные особенности
1. Модульная загрузка (Header/Footer)

Для предотвращения дублирования кода используется скрипт main.js, который подтягивает содержимое файлов из папки /partials/ в специальные контейнеры <div id="header-placeholder"></div>. Это позволяет редактировать меню один раз для всего сайта.
2. Система локализации (i18n)

Логика перевода вынесена в translations.js. Система работает в три этапа:

    Детекция: Проверка сохраненного языка в localStorage.

    Домен: Если данных нет, скрипт проверяет TLD домена (.fi -> FI, .ee -> ET).

    Рендер: Поиск всех элементов с атрибутом data-lang и замена их текстового содержимого.

3. Настройка двух доменов

Проект спроектирован для работы на одном хостинге с привязкой двух доменов. Внутренняя логика автоматически подстраивает контент (названия компании Fimarek Oy / Fimarek OÜ) в зависимости от текущего URL.